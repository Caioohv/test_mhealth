the goal of this project is to test my mobile health api

i think that this project should have one page only, just with some divs

the first div is login/register

when register, should create some fake data for the user, like name, email
email should be easy, as 'amanda@gmail.com', 'ederson@gmail.com', etc.
password should always be 'senha1'

when someone open this website, it should show in the login / register section the current users (get /api/dev/users)

when someone click in login, should login and show the user data in the screen.
like 'Acessando como Amanda'
also, the site should be in portuguese brazil

the user should be capable of alternating between logins, so, if someone click in another user, should login as that user

when logged with some user, show the current user networks, members, permissions, and allow the action to invite someone by email and set its permissions

should allow to create networks too

in the next section, show current invites.

if i invite alana@gmail.com, when i log in with alana@gmail.com, i should see a invite section with the invite showing the permissions and etc

